import { HashPasswordPipePipe } from './hash-password-pipe.pipe';

describe('HashPasswordPipePipe', () => {
  it('should be defined', () => {
    expect(new HashPasswordPipePipe()).toBeDefined();
  });
});
